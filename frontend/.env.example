# Copy to .env.local. The FastAPI backend base URL used by the Next.js API proxy.
BACKEND_API_BASE=http://localhost:8000

# Backward-compatible fallback for older deployments.
NEXT_PUBLIC_API_BASE=http://localhost:8000
